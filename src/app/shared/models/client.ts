export class Client {

    constructor(public id: number, public sharedKey: string, public name: string, public phone: string, public email: string, public startDate: string, public endDate: string) {
    }

}
